# 项目协作规范

## 构建与演示文件约定

- 微信小游戏统一导出到本仓库的 `build/wechat/`，直接覆盖已有生成工程；不要按功能另建微信导出目录。使用 `python3 tools/export_wechat.py`，不另传 `--output`。
- 默认只生成微信工程目录，不生成 `wechat.zip`。仅在用户明确要求 ZIP 时添加 `--zip`。
- GIF、MP4、截图等功能演示文件统一归档到 `build/previews/YYYY-MM-DD/<功能名>/`；同日同功能多次迭代时加构建号区分。演示文件不得放进 `build/wechat/`，也不提交构建产物。
- 覆盖导出前关闭微信开发者工具中当前烧烤工程，完成后重新打开 `build/wechat/`，避免旧文件列表缓存；不要关闭其他项目。

## Git 提交约定

- 每次完成代码修改并通过与变更相关的验证后，创建本地 Git commit；如果用户明确要求暂不提交，则遵循用户要求。
- 提交必须使用 **两个 `-m` 参数**：第一个为 Commit Header，第二个为包含全部多行列表的 Body。Body 用一对单引号或双引号包裹，保留实际换行；不要把每条列表拆成多个 `-m`。
- Header 使用 Conventional Commits 类型与简洁中文描述，例如 `feat:`、`fix:`、`refactor:`、`docs:`、`chore:`。
- Body 用中文 `- ` 列表说明本次实际变化及相关验证。示例中的功能不是额外开发需求。
- 只提交当前任务涉及的文件，不夹带不相关修改；不提交 `.godot/`、构建产物、系统元数据或密钥。
- 推送到远端按用户授权范围执行，不强制推送或改写已有历史。

示例：

```bash
git commit -m "feat: 调整对局 HUD 并预留底部功能区" -m "- 调整关卡、倒计时和出炉进度的布局与样式。
- 预留四个功能位和广告区域。
- 完成相关界面验证，保留现有关卡进度。"
```

## 验证

- 核心规则变更运行 `tests/run_tests.gd`；输入、页面流程与布局变更按需要运行 `tests/ui_smoke.gd`。
- 低风险文案或样式调整优先用导入检查和实际画面验证，不添加仅复述改动的测试。
- 复用未受影响的已通过结果；在提交说明中准确区分已验证内容和仍需真机验收的部分。
