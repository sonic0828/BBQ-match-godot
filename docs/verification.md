# 首版验证记录

日期：2026-09-23。引擎：Godot 4.7.2.stable，macOS Apple M4 Pro，Compatibility / OpenGL。

## 自动验证结果

- `tests/run_tests.gd`：**78 项检查通过，退出码 0**。
- `tests/ui_smoke.gd`：**18 项真实场景输入／导航检查通过，退出码 0**。
- 主场景 headless 启动通过；原生图形运行和截图日志未出现脚本或资源错误。

规则覆盖：拖拽保留源数据、取消回滚、空位 Move、目标有食材且仍有其他空位时 Swap、同烤架拒绝、目标版本过期拒绝、动画局部锁、双消同帧开始、其他烤架并行操作、主动搬空补货、1／2／3 串布局、每次只补一碟、补货自动连消、教程停表、暂停冻结、超时取消拖拽、超时优先于末次结算、本地存储恢复与非法值处理。

界面覆盖：从真实主场景实例通过 Viewport 分发鼠标按下／移动／松开，完成 Lv.1 教学移动和 Lv.3 双消；暂停／继续、切后台、前台保持暂停、超时面板、首页、选关、设置。360×640、390×844、768×1024 三种窗口尺寸分别检查舞台边界和拖拽双消。

测试输入在 Viewport 本地坐标空间分发；初次使用 `Input.parse_input_event` 时坐标空间不匹配，已在测试入口修正。生产输入路径未用测试结果掩盖坐标转换。

## 10 关可解性

自动操作走实际 `begin_drag` / `drop` / `tick` 规则，等待独立烤架结算，最终检查棋盘和所有补货队列均为空。为独立验证可解性，自动通关测试将时间放宽，不代表真实玩家在默认时限内一定能通关。

| 关卡 | 自动操作次数 | 最终消除组数 |
| --- | ---: | ---: |
| 1 | 6 | 5 / 5 |
| 2 | 15 | 10 / 10 |
| 3 | 16 | 12 / 12 |
| 4 | 17 | 12 / 12 |
| 5 | 24 | 15 / 15 |
| 6 | 26 | 16 / 16 |
| 7 | 25 | 18 / 18 |
| 8 | 30 | 20 / 20 |
| 9 | 32 | 20 / 20 |
| 10 | 36 | 22 / 22 |

## 实际画面

原生 Godot 窗口截图已检查中文、透明素材、棋盘、下一碟预览、HUD 和教学提示。

- `screenshots/home.png`
- `screenshots/level3.png`
- `screenshots/level9-compact.png`

### UI 调整验收

- 名称更正为「烧烤串串消」，对局页头与底部口号移除。
- HUD 按新参考图采用单行关卡／蓝色金边时钟框／出炉组数／圆形暂停按钮。
- 四个锁定功能位与空白广告区位于底部；广告区 568×86 逻辑像素，约 6.6:1。功能及广告业务尚未接入。
- 对局舞台在长屏上使用完整可用高度，调整烤架行距和底部位置；暂停面板也随舞台居中。
- 18 项既有界面与输入检查通过；390×844 长屏及 450×800 普通竖屏实际渲染已检查，HUD、补货碟、功能位无重叠。
- 已验证旧名称存档迁移：第 5 关解锁、前 4 关完成状态保留；新旧存档内容相同，原存档校验值未变。

## 尚未验证

- 微信／抖音宿主、iOS／Android 真机触摸、触摸取消与刘海安全区。
- 真机 60 FPS、低端设备内存、引擎与资源包体预算。
- Web／原生平台导出产物；本机未安装导出模板。
- 真实玩家的关卡难度、时限和连续长时间游玩手感。
- 震动实际设备效果，以及小游戏宿主的存储／音频恢复。

沙盒内 headless Godot 启动时有 macOS 系统证书读取提示；测试退出码为 0，游戏未使用网络功能。原生图形运行未出现该提示。测试等待末个音效播放结束后退出，最终结果无音频资源残留告警。

本次没有发布或上传到任何平台，也没有修改引擎安装版本。

## 2026-09-26 成盘动效验证

- 当前全部 8 种食材增加约 0.95 秒的张开蓄力、收拢、闪光成盘、悬停和淡出。连续自动消除时，上一盘提前淡出，为下一盘让出展示位置。
- 补位保留原有 0.32 秒时长，按食材顺序间隔 0.05 秒起飞；从实际预览位置和尺寸移动、放大到槽位。1／2／3 串分别进入中央／两侧／全部槽位。下一盘在补位完成后渐显。
- 动效使用食材快照；暂停冻结，超时清理，重开／离开页面不残留。胜利规则与存档仍立即处理，仅延后胜利面板和音效到最后成盘收尾，期间不消耗倒计时。
- `tests/run_tests.gd`：78 项规则检查通过，10 关可解性结果不变。
- `tests/ui_smoke.gd`：18 项真实场景输入／导航检查通过，涵盖三种窗口尺寸。修正亚像素星芒的退化多边形后，未再出现绘制错误。
- `tests/combo_animation.gd`：48 项动效与生命周期检查通过；原生 Compatibility 渲染模式另外验证完整时间轴推进，共 49 项。已逐帧检查 720×1280 窗口中的 8 种成盘和补位，包含密集三行棋盘。
- 可复现预览：`Godot --path . --script tests/combo_animation.gd -- --test-session --capture-combo=/private/tmp/bbq-combo-frames`。该显式录制模式逐帧推进演示场景并保存 PNG，不改变正式关卡数据。
- 首次曾导出到 `build/combo-preview/wechat` 并生成 ZIP（约 14.00 MiB 工程），现已移至 `build/archives/2026-09-26/combo-preview/` 留档，后续统一使用 `build/wechat/`。该次导出日志确认新图集已入包；资源包在本地 Godot headless 模式下进入第 3 关，无脚本或资源加载错误。该检查不代表微信宿主渲染验收。
- 预览视频及 GIF 已归档到 `build/previews/2026-09-26/food-combo/combo-animation.mp4` 和 `.gif`，是本地实际渲染画面的 8 种食材并行动效演示；构建产物不提交。
- 原生渲染／导出日志无错误；沙盒 headless 检查仍有既有 macOS 系统证书读取提示，未影响离线检查。未重新上传微信预览，本次新增动效的 iPhone 真机表现和性能仍待验收。

## 2026-09-26 固定导出目录与演示归档

- 已将固定覆盖 `build/wechat/`、默认不生成 ZIP、演示按日期／功能归档写入 `AGENTS.md` 和构建文档。脚本新增显式 `--zip` 开关，普通命令不生成或更新 ZIP。
- 普通构建 `20260926-180037` 导出成功，工程 14,680,629 字节；`build/wechat.zip` 不存在，工程内不含 GIF／MP4／ZIP。旧 ZIP 与独立导出已保存在 `build/archives/2026-09-26/`，演示保存在 `build/previews/2026-09-26/food-combo/`。
- 已核对构建清单大小和资源 CRC32，资源包 SHA-256 与前次通过验证的成盘版本一致，因此复用游戏规则和动效测试结果。
- 关闭当前烧烤工程后覆盖导出，再重新打开同一 `build/wechat/`。模拟器确认最新构建号、场景就绪、首帧及首页显示，启动错误 0 条；两条现有提示分别为平台 HarmonyOS 接口提示与 ScriptProcessorNode 弃用警告。未生成新的手机预览二维码。
