# 平台准备与边界

核实日期：2026-09-24。

## 微信

已经使用 `godothub/godot-minigame` 的 4.7.0.8 模板，导出 `build/wechat/` 和 `build/wechat.zip`。AppID 为 `wxd575463c13869e7d`。

游戏使用 Godot 4.7.2、GDScript、2D Compatibility、单线程；通过脚本打包资源并组装微信适配运行时。已在开发者工具实际验证首屏、第一关拖拽通关、第二关入口、重启存档恢复。手机音效、前后台与性能仍需真机验收。

版本差异、可复现导出命令、验证截图和后续验收项见 [微信导出说明](wechat_export.md)。

## 普通 Web / 抖音

保留 `Web Preview` 单线程竖屏预设。本机没有官方 Web 导出模板，尚未交付普通浏览器版本。微信包使用专用适配器，不能直接作为普通 Web 或抖音发布包。

抖音尚未适配、导出或验收；应独立核对对应的 `tt` 运行时、存储、触摸、音频和发布要求。

## TapTap / Steam（第二阶段候选）

保持核心规则、关卡与画面复用。确定发布方向后再补原生导出模板、安卓签名与包名、桌面分辨率体验及对应平台能力。当前未集成这些平台 SDK。
